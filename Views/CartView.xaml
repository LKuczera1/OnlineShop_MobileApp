<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OnlineShop_MobileApp.Views.CartView">
    <Grid RowDefinitions="3*,1*" Padding="12">

        <!-- LISTA -->
        <CollectionView Grid.Row="0"
                    ItemsSource="{Binding Items}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,0,0,10" Padding="12">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">

                            <!-- lewa część: opis produktu -->
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontSize="18"/>
                                <Label Text="{Binding Subtitle}" FontSize="13"/>
                            </VerticalStackLayout>

                            <!-- prawa część: kontrolki ilości + usuń -->
                            <HorizontalStackLayout Grid.Column="1" Spacing="8" VerticalOptions="Center">
                                <Button Text="-" WidthRequest="44" HeightRequest="44"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}},
                                                      Path=BindingContext.DecreaseCommand}"
                                    CommandParameter="{Binding .}" />

                                <Frame WidthRequest="44" HeightRequest="44" Padding="0" BorderColor="#777">
                                    <Label Text="{Binding Quantity}"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"/>
                                </Frame>

                                <Button Text="+" WidthRequest="44" HeightRequest="44"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}},
                                                      Path=BindingContext.IncreaseCommand}"
                                    CommandParameter="{Binding .}" />

                                <Button Text="U" WidthRequest="44" HeightRequest="44"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentView}},
                                                      Path=BindingContext.RemoveCommand}"
                                    CommandParameter="{Binding .}" />
                            </HorizontalStackLayout>

                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- DÓŁ: podsumowanie / checkout (placeholder) -->
        <Frame Grid.Row="1" Padding="12">
            <Label Text="Podsumowanie / checkout (na później)" />
        </Frame>

    </Grid>

</ContentView>